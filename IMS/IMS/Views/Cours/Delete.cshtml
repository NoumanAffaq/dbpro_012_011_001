@model IMS.Models.Cours

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Cours</h4>
    <hr />
    <div class="advanced-search" style="font-size:20px">
        <div class="col-md-6">
            @Html.DisplayNameFor(model => model.CourseName)
        </div>

        <div class="col-md-6">
            @Html.DisplayFor(model => model.CourseName)
        </div>

        <div class="col-md-6">
            @Html.DisplayNameFor(model => model.CreditHours)
        </div>

        <div class="col-md-6">
            @Html.DisplayFor(model => model.CreditHours)
        </div>

        <div class="col-md-6">
            @Html.DisplayNameFor(model => model.CourseFee)
        </div>

        <div class="col-md-6">
            @Html.DisplayFor(model => model.CourseFee)
        </div>

        <div class="col-md-6">
            @Html.DisplayNameFor(model => model.StartTime)
        </div>

        <div class="col-md-6">
            @Html.DisplayFor(model => model.StartTime)
        </div>

    </div>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
           <button> @Html.ActionLink("Back to List", "Index") </button>
        </div>
    }
</div>
